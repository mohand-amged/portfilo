export type Testimonial = {
  quote: string;
  name: string;
  image: string;
  link: string;
  demo?: string;
};

export type skills = {
  name: string;
  description: string;
  icon: React.ReactNode;
};
